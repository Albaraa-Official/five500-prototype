# مراجعة شاملة — FIVE 500 (2026-07-22)

مراجعة كاملة عبر 6 أبعاد + نتائج اختبار التحمّل. تُقرأ قبل الإطلاق.

## 1) اختبار التحمّل (نسخة إنتاج + Postgres)
| متزامن | نجاح | إنتاجية | p50 | p95 |
|--------|------|---------|-----|-----|
| 20 | 20/20 | 236/ث | 58ms | 72ms |
| 50 | 50/50 | 456/ث | 76ms | 93ms |
| 100 | 100/100 | 672/ث | 88ms | 135ms |
| 200 | 200/200 | 900/ث | 134ms | 207ms |

- **سلامة البيانات:** 370 طلب كلها paid، 0 إجمالي خاطئ، لا سباقات (race conditions) ولا فقدان كتابة.
- **البيئة:** جهاز محلي، Postgres واحد. الأرقام إرشادية؛ Vercel serverless يختلف (انظر بند 6).

## 2) الأمان ✅ / ⚠️
- ✅ التسعير من السيرفر — تلاعب السعر مُختبَر ومرفوض.
- ✅ جلسة HMAC HttpOnly + SameSite، مقارنة ثابتة الزمن.
- ✅ OTP مُجزّأ، حد محاولات، انتهاء صلاحية.
- ✅ Zod strict على المدخلات، Prisma (لا SQL خام).
- ✅ webhook idempotent، mock-confirm معطّل في الإنتاج.
- ✅ رؤوس أمان، Next مرقّع للحرِج.
- ⚠️ **إلزامي قبل الإطلاق:** ضبط `MOYASAR_WEBHOOK_SECRET` — بدونه الـwebhook يقبل بلا توقيع.
- ⚠️ **إلزامي:** `SESSION_SECRET` قوي عشوائي في Vercel.
- ⚠️ حد المعدّل في الذاكرة — لا يعمل عبر نسخ serverless متعددة؛ يحتاج Upstash Redis.

## 3) الصحّة (Correctness)
- ✅ مال بالهللات (int) — لا أخطاء فاصلة عائمة.
- ✅ حالات الطلب واضحة؛ الانتقال إلى paid من webhook فقط.
- ✅ اختبارات وحدة 4/4 (ضريبة، تطبيع جوال).
- ⚠️ لا يوجد بعد انتقال preparing→ready→completed (يحتاج لوحة إدارة/مطبخ).

## 4) الأداء
- ✅ زمن استجابة ممتاز حتى 200 متزامن.
- ⚠️ الصور `<img>` بدل `next/image` — يؤثر على LCP؛ يُنصح بالتحويل.

## 5) قابلية الصيانة
- ✅ فصل واضح: lib (منطق)، app/api (مسارات)، context (حالة).
- ✅ توثيق كامل في docs/.
- ✅ مصدر واحد للحقيقة (DB) عبر seed من data/menu.js.

## 6) قابلية التوسّع — ⚠️ أهم بند لـ Vercel
- **Prisma + serverless:** كل استدعاء دالة يفتح اتصالاً؛ تحت الضغط تُستنزف اتصالات Postgres. **الحل الإلزامي:** استخدم **رابط Postgres المُجمّع (pooled)** — Vercel Postgres يوفّره تلقائياً، أو Neon pooling، أو Prisma Accelerate. بدونه اختبار الـ200 متزامن قد يفشل على Vercel رغم نجاحه محلياً.
- حد المعدّل يحتاج مخزناً مشتركاً (Redis).

## البنود المفتوحة (حسب الأولوية)
1. 🔴 رابط Postgres مُجمّع على Vercel (قبل أي حمل حقيقي).
2. 🔴 MOYASAR_WEBHOOK_SECRET + SESSION_SECRET.
3. 🟠 مزود OTP حقيقي + مفاتيح Moyasar.
4. 🟠 Redis لحد المعدّل.
5. 🟡 next/image، ترقية Next 16، لوحة إدارة، ZATCA، واجهة متجاوبة كلياً.

## الخلاصة
الأساس **متين وآمن ومختبَر تحت الضغط**. جاهز للنشر على Vercel بمجرد ضبط: Postgres مُجمّع + الأسرار + مزودات OTP/الدفع الحقيقية.
