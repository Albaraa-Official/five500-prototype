export default function Loading() {
  return (
    <div className="app pad" style={{ display: "flex", alignItems: "center", justifyContent: "center", minHeight: "70vh" }}>
      <span className="muted" style={{ fontSize: 14, fontWeight: 700 }}>جاري التحميل…</span>
    </div>
  );
}
